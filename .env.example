# Copy this file to .env and replace the placeholder with the PostgreSQL URI
# shown by Supabase's Connect dialog for the BirdBrain DEVELOPMENT project.
# .env is gitignored. Never commit the real password or URI.
BB_DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/postgres?sslmode=require
